[Unit]
Description=Daemon to Seafile hub daemon
After=syslog.target

[Service]
ExecStart=<%= seafile::base_dir %>/seafile-server-latest/seahub.sh restart
Type=forking

[Install]
WantedBy=multi-user.target
