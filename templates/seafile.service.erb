[Unit]
Description=Daemon to Seafile
After=syslog.target zfs.target mariadb.service

[Service]
ExecStart=<%= scope.lookupvar('seafile::base_dir') %>/seafile-server-latest/seafile.sh restart
Type=forking

[Install]
WantedBy=multi-user.target
